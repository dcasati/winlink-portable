[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Winlink Portable Ops"
authors = [{name = "Diego Casati", email = "diego.casati at gmail dot com"}]
dynamic = ["version", "description"]